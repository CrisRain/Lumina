I will implement automatic kernel version management for `usque`, including version detection, update checking, and automatic updates.

### Backend Implementation (`controller-app/app/kernel_version_manager.py`)
1.  **Enhance `KernelVersionManager`**:
    *   Add `get_current_binary_version(binary_path)`: Run `${binary_path} version` to get the actual version of the binary. Handle potential parsing variations (assuming standard semantic version output like `usque version x.y.z`).
    *   Add `check_for_updates(current_version)`:
        *   Fetch release info from `https://api.github.com/repos/Diniboy1123/usque/releases/latest`.
        *   Compare semantic versions.
    *   Add `download_and_install(version, asset_url)`:
        *   Download the zip file.
        *   Extract to `data/kernels/usque/<version>/`.
        *   Make executable.
    *   Add `auto_update()`:
        *   Orchestrate the check and download process.
        *   If a new version is found and downloaded, automatically set it as active (optional, or just notify).
        *   Update the `latest` symlink or configuration.

### Startup Integration (`controller-app/app/main.py`)
1.  **Background Update Task**:
    *   In the `@app.on_event("startup")` handler, schedule `KernelVersionManager.auto_update()` to run in the background.

### Frontend Implementation (`frontend/src/views/KernelManager.vue`)
1.  **Update UI**:
    *   Display "Checking for updates..." or "Update Available".
    *   Add a manual "Check for Updates" button.
    *   Show release notes if available (optional, link to GitHub).

### Dependencies
-   `requests` is already in `requirements.txt`.
-   Need `zipfile` (standard lib).

### Note on Platform
-   The implementation will primarily target Linux (Docker container), as the release assets are likely platform-specific (`linux_amd64`, etc.).
-   I will add logic to detect the architecture (`amd64`, `arm64`) to select the correct asset.

### Verification
-   Since I cannot run the binary in this environment (Windows), I will write robust parsing logic and log errors if execution fails.
-   I will verify the file structure creation and config updates.
