# Lumina / WarpPanel Environment Configuration Template
# Password and panel configuration are initialized from the web setup page
# and persisted in SQLite at /app/data/config.db.

# Backend option: 'usque' (default), 'official'
WARP_BACKEND=usque

# Optional CORS allow-list (comma separated)
# CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional auth controls
# AUTH_TOKEN_TTL_SECONDS=43200
# AUTH_MAX_LOGIN_ATTEMPTS=10
# AUTH_ATTEMPT_WINDOW_SECONDS=300
# AUTH_BCRYPT_ROUNDS=12

# Runtime overrides (generally not needed once initialized)
# SOCKS5_PORT=1080
# PANEL_PORT=8000

# Optional panel TLS (HTTPS for uvicorn directly)
# PANEL_SSL_ENABLED=true
# PANEL_SSL_CERT_FILE=/app/data/ssl/panel.crt
# PANEL_SSL_KEY_FILE=/app/data/ssl/panel.key
# PANEL_SSL_AUTO_SELF_SIGNED=true
# PANEL_SSL_DOMAIN=localhost
# PANEL_HTTP_REDIRECT_STATUS=308
# PANEL_HTTP_REDIRECT_FORCE_DOMAIN=
# PANEL_HTTP_HTTPS_MUX_ENABLED=true
# PANEL_HTTPS_INTERNAL_PORT=8443

# Optional official backend log controls (reduce docker logs noise)
# WARP_SVC_RUST_LOG=warn
# WARP_SVC_LOG_MODE=file
# WARP_SVC_LOG_FILE=/var/log/warp-svc.log
# WARP_SVC_LOG_MAX_BYTES=10485760
# WARP_SVC_LOG_KEEP_FILES=5
# WARP_SVC_LOG_CHECK_INTERVAL=30
