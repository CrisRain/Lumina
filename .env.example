# Lumina / WarpPanel Environment Configuration Template
# This file provides examples of environment variables that can be used to configure the application.
# Environment variables defined here (or passed via docker-compose) will always have strictly higher 
# priority than the user settings saved in config.json.

# Backend option: 'usque' (default), 'official'
WARP_BACKEND=usque

# The password required to access the Web UI.
# If left empty, no authentication is required.
# PANEL_PASSWORD=secret

# SOCKS5 Proxy Port (Default is 1080)
# Uncomment and change this to override the proxy port via environment.
# SOCKS5_PORT=1080

# Web UI Panel Port (Default is 8000)
# Uncomment and change this to override the Web UI port via environment.
# PANEL_PORT=8000
