# Lumina / WarpPanel Environment Configuration Template
# Password and panel configuration are initialized from the web setup page
# and persisted in SQLite at /app/data/config.db.

# Backend option: 'usque' (default), 'official'
WARP_BACKEND=usque

# Optional CORS allow-list (comma separated)
# CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Optional auth controls
# AUTH_TOKEN_TTL_SECONDS=43200
# AUTH_MAX_LOGIN_ATTEMPTS=10
# AUTH_ATTEMPT_WINDOW_SECONDS=300
# AUTH_BCRYPT_ROUNDS=12

# Runtime overrides (generally not needed once initialized)
# SOCKS5_PORT=1080
# PANEL_PORT=8000
